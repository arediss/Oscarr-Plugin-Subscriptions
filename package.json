{
  "name": "oscarr-plugin-subscription",
  "version": "0.1.2",
  "description": "Subscription tracking plugin for Oscarr — tiers, user assignments, expiration notifications, automatic role downgrade.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/react": "^18.3.0",
    "esbuild": "^0.28.0",
    "tailwindcss": "^3.4.17",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "lucide-react": "^1.8.0"
  }
}
